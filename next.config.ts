import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['localhost','youtube.com','www.youtube.com', 'www.imagesoft.ettora.com','lh3.googleusercontent.com','images.pexels.com', 'cdn.staticans.com' ,'encrypted-tbn0.gstatic.com', 'imagesoft.ettora.com'], // Add the domain of the image source here
  },
  /* config options here */
};

export default nextConfig;
